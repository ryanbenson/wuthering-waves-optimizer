# Website ID from your Umami Cloud account (Settings > Websites). Set this in
# Vercel's Production (and Preview, if desired) environment variables — analytics
# are a no-op locally/in Cypress when unset. See src/utils/analytics.ts.
VITE_UMAMI_WEBSITE_ID=
