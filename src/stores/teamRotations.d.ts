/**
 * Ambient types for the Pinia teamRotations store (implementation is teamRotations.js).
 * Typed as StoreGeneric so helpers like storeToRefs accept it.
 */
import type { Pinia, StoreGeneric } from "pinia";

export declare function useTeamRotationsStore(pinia?: Pinia): StoreGeneric;
